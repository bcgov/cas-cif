apiVersion: v1
kind: Secret
metadata:
  name: gcp-{{ .Values.host.namespace }}-cif-backups-service-account-key
  labels: {{ include "backup-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
type: Opaque
data:
  credentials: {{ .Values.host.secret.credentials | b64dec | quote }}
