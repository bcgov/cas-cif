describe("The Project Emission Intensity Report Form Summary Component", () => {
  it(
    "displays a 'not added' message when there was no TEIMP information added"
  );
  it("displays a 'not updated' when there were no updates to the form");
  it("only displays the fields that were updated on the summary");
});
